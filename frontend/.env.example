# Copie este arquivo como frontend/.env e ajuste se necessario.
# O .env real fica fora do git (ver frontend/.gitignore).
#
# IMPORTANTE: o frontend aponta APENAS para staging nesta fase.
# Nao trocar para a URL de producao sem autorizacao explicita.

VITE_API_BASE_URL=https://api-de-coworking-staging.onrender.com
